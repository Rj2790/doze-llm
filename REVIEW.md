Review complete. Tests pass (64 passed). Findings below, ordered by severity, each tagged with the check number it answers. DEVIATIONS.md does not exist, so check 6 has nothing to compare against beyond CONTEXT.md §4.

Would silently invalidate a comparison or break a PREREG guarantee

- A1 (check 7). Seeds do not fix dream sampling or LoRA init. PREREG.md:144-145 promises both. hf_backend.py:59-65 samples with do_sample=True and never seeds torch or passes a generator; hf_backend.py:82 lets PEFT init lora_A from the unseeded global RNG; the constructor at hf_backend.py:23-24 has no seed argument and modal_app.py:54 passes none. MLX is the same at mlx_backend.py:53 and mlx_backend.py:73. Two Sleep runs with the same seed will produce different dreams and different adapters. No test covers same-seed reproducibility. Even after seeding, "bit-identical" on CUDA also needs deterministic algorithms; the realistic target is "fully seeded", which should be the stated claim.
- A2 (checks 3, 6). filter_like_sleep breaks the step match by construction. online.py:34-36 skips the gradient step on filtered episodes while Sleep trains S=K steps regardless (consolidate.py:52). At roughly half kept, Online would take about half of Sleep's 600 steps. With the default False the count is exactly K per K episodes (online.py:39, asserted by tests/test_sleep_arms.py:34). If the pending decision goes toward filtering, the matching rule needs a redesign, not a flag. Whichever way it goes, it must be written into a DEVIATIONS.md that currently does not exist (CONTEXT.md:283).
- A3 (check 3). check_matched is advisory only. It runs once per seed at the end of grid (modal_app.py:113), catches the exception and prints (modal_app.py:115-116). No run JSON records the verdict and no analysis script exists, so PREREG.md:102-103 "refuses to compare" is not enforced anywhere. The backend-mix guard at compute_ledger.py:92-94 is trivially satisfied in the grid because modal_app.py:109 hardcodes the backend string instead of reading it from the run. Persist a matched verdict per seed and have the analysis load ledgers via Ledger.load.
- A4 (check 3). Training-token match Online vs Sleep is likely to fail post hoc. Online trains on every raw trajectory including 400-token truncations (online.py:39, harness.py:41); Sleep trains only on kept and verified completions near the 97-token median (CONTEXT.md:232). The 5% tolerance on training tokens is a real risk and would only surface after a full seed. Estimate Online's mean trajectory length from the Baseline run's episode tokens before launching the trainable arms.
- A5 (checks 1, 4). No episode-0 checkpoint. PREREG.md:136 defines forgetting relative to episode 0; harness.py:211 only checkpoints at multiples of K. Borrowing Baseline's control accuracy is valid only because lora_B starts at zero and decoding is greedy. Either add a pre-loop checkpoint or write that substitution down explicitly.
- A6 (check 1). Train and held-out answer distributions are not "exactly uniform" as PREREG.md:67-68 states. number_reduction.py:150-154 takes the first 800 and 200 of a shuffled pool. Measured on seed 0:

| split    | answer 1 | answer 4 | answer 9 |
|----------|----------|----------|----------|
| train    | 235      | 278      | 287      |
| held-out | 79       | 66       | 55       |
  The probe is stratified (shortcut_detector.py:62-80) so the primary metric is safe, but held-out accuracy has a constant-answer ceiling near 40% rather than 33%. Stratify the split or amend PREREG before freeze.

Would crash or lose a run

- B1. Modal timeout of 6h (modal_app.py:75) is probably too short. Each checkpoint generates 560 items one prompt at a time (hf_backend.py:56), 300 of them GSM8K at up to 512 tokens. A full 600-episode Sleep run plausibly needs 8 to 12 hours, and results are saved only at the end (modal_app.py:66-69). Raise the timeout to 24h, save per checkpoint, and batch generation.
- B2. Two @app.local_entrypoint() functions (modal_app.py:84, modal_app.py:93). The documented command at modal_app.py:4-5 will fail with an ambiguous-entrypoint error; it needs modal_app.py::main.
- B3. dtype= kwarg at hf_backend.py:35 is accepted by transformers 4.56 and later; the pin at modal_app.py:29 is only >=4.51. An older resolve would raise or silently load fp32. Assert model.dtype == torch.bfloat16 at startup.
- B4. Environment prerequisites. modal.Secret.from_name("huggingface") at modal_app.py:76 must exist in the workspace; modal is not in requirements.txt; running Awake via main with the default awake_budget=0 raises at awake.py:34-35 only after the model has loaded.

Cosmetic and hygiene

- C1 (check 2). Dream leak checks live only in dreamer.py:73; consolidate.py:40 checks kept alone and buffer.add(new) at consolidate.py:60 runs after backend.train at consolidate.py:54. No leak is possible today because train and held-out prefixes partition all 2187 seven-digit strings (verified) and verify enforces length 12 (dreamer.py:64), but move a buffer.check(new) before training for defence in depth.
- C2 (check 1). Dreams enter the replay buffer (consolidate.py:60); PREREG.md:115 step 7 says "kept trajectories". Reading choice, should be documented.
- C3 (check 5). The keep rule at filters.py:30 keeps any trajectory with a correct final answer regardless of chain quality. Calibration shows 47.5% correct answers but only 17.5% exact chains, so many kept trajectories carry wrong steps. Literal PREREG; add it to the CONTEXT.md:212-214 note.
- C4 (check 3). Awake runs critique rounds on probe prompts at eval (awake.py:57) with a critique that assumes work-format output (awake.py:23-27) while the probe asks only for ANSWER. Intentional per CONTEXT.md:201-203, but it is a protocol difference on the primary metric and belongs in the write-up.
- C5 (check 8). HF sampling sets only top_p (hf_backend.py:61), so Qwen3's default top_k=20 stays active; MLX uses pure temperature. Never mixed, but set top_k=0 so "temperature 0.8" is literally true.
- C6. Repo state vs CONTEXT.md §6: results/runs/ does not exist though CONTEXT.md:280 and CONTEXT.md:310 reference it; PREREG.md:72 commit hash blank; no git repo.
- C7. _run in modal_app.py:70-72 returns no backend name; the run JSON does carry it (harness.py:97). Probe items are a subset of the 200 held-out eval items (harness.py:148-149), so the two accuracies are correlated.

Checks that pass, with citations. L=12 (harness.py:38); mirror indices verified at number_reduction.py:113; 437 of 2187 prefixes held out (number_reduction.py:146-147); probe masks digits 8 to 12 with target r6 (number_reduction.py:65-67, number_reduction.py:92-94, number_reduction.py:265-272); criterion 0.70 on two consecutive checkpoints (shortcut_detector.py:114-126); K=50, 600 episodes, 60 probe items (harness.py:36-43); 5 seeds (modal_app.py:94); thinking off (hf_backend.py:48-49); LoRA r=16, alpha 32, dropout 0, all seven projections, Adam 1e-4 identical across backends (training_utils.py:16-22, mlx_backend.py:73-76, hf_backend.py:80-83); lora_A init bounds identical at 1/sqrt(fan_in) (mlx_backend.py:135, hf_backend.py:132); loss mask equivalence confirmed against installed mlx_lm 0.31.3 default_loss. Episode sequence is train-only and seeded (harness.py:134-143); replay buffer raises LeakError (replay_buffer.py:29-38); checkpoints call attempt with phase="eval" and after_episode runs only in the day loop (harness.py:154-183, harness.py:210). First night covers episodes 1 to 50 and the last 551 to 600 with an assertion (harness.py:212-213); night completes before the checkpoint (harness.py:214-217); Online's final step precedes the final checkpoint. Every dream passes through verify (dreamer.py:92-99) which checks answer, exact STEPS and every work line (dreamer.py:39-80); rejected dreams count toward generated tokens (dreamer.py:93, consolidate.py:46), which is correct under PREREG.md:99, and never toward training tokens since only train_set reaches backend.train. Ledger writes: day tokens harness.py:206, Online steps online.py:40, dream tokens consolidate.py:46, night steps consolidate.py:61, snapshots harness.py:182, eval tokens kept separate harness.py:162-170. Awake budget derives from Sleep's total including dreams and accrues cumulatively (awake.py:42-44, awake.py:53-55).

First-execution validation list for hf_backend.py and modal_app.py.
1. model.dtype is bfloat16; adapter params fp32; trainable count near 33M for 36 layers.
2. Chat template emits the empty think block; generation stops at <|im_end|>; completion_tokens agrees with count_tokens(text) to within one.
3. Two greedy calls on one prompt are identical.
4. train on one repeated example: finite decreasing loss, training_tokens equals completion length plus one, lora_norm grows, decay_adapter(0.05) shrinks it by exactly 5%, reset_adapter returns it to zero.
5. starmap returns three results; volume commit persists under /results/runs; HF cache is reused on the second run.
6. Wall-clock per episode and per checkpoint, to size the timeout.

Recommendation: no-go for the seeded grid; go for the 4-episode Baseline path check using modal_app.py::main, which is already CONTEXT.md §8 step 6. Before any recorded run: fix A1 seeding, decide A2 and write DEVIATIONS.md, persist the A3 verdict, resolve A5 and A6, and raise the B1 timeout. The frozen items themselves are implemented correctly; the blockers are reproducibility, matching enforcement, and infrastructure.